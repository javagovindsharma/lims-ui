// reducers/authReducer.ts
import { AuthAction } from '../actions/authActions'; // Adjust the path accordingly
import { AuthState } from '../store/authentication/types'; // Adjust the path accordingly


const authReducer = (state:AuthState, action: AuthAction): AuthState => {
      return state;
};

export default authReducer;
